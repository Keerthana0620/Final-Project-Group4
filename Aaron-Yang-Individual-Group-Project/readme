project_root/
├─ data/ 
│  ├─ raw/                       # Original raw data files (CSV, etc.)
│  ├─ processed/                 # Processed data outputs (from data_preprocessing)
│  └─ splits/                    # Train/Val/Test splits after prepare_data step
│
├─ scripts/
│  ├─ data_preprocessing.py      # Already have: Cleans & normalizes raw data, outputs processed_for_t5.csv
│  ├─ prepare_data.py            # Takes processed_for_t5.csv, creates prompt-target pairs, splits into train/val/test
│  ├─ run_training.py            # Fine-tunes T5 model on train set, evaluates on val, saves model checkpoints
│  ├─ run_generation.py          # Uses the fine-tuned T5 model to generate text from a prompt
│  ├─ evaluation.py              # Script to evaluate the model on the test set 
│  └─ utils/                     # Utility functions (tokenization helpers, metric calculation, logging setup, etc.)
│
├─ models/
│  ├─ checkpoints/               # Saved model checkpoints from training
│  └─ final/                     # Final fine-tuned model and tokenizer
│
├─ configs/                      # Configuration files for training hyperparams, model paths, etc.
│  ├─ training_config.json
│  └─ generation_config.json
│
└─ requirements.txt              # Python dependencies
